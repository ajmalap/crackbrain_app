name: quiz_app_ii_example
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_svg: ^0.19.1

  # icons
  font_awesome_flutter: ^8.11.0
  firebase_core:
  firebase_analytics:
  firebase_auth:
  cloud_firestore:
  url_launcher: ^6.0.9


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/images/
    - assets/icons/